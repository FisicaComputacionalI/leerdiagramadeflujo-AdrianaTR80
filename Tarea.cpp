// Tarea leer diagrama de flujo
// Adriana Tapia Ramírez

# include  < iostream >

using namespace  std ;

int  main ( ){
    int s = 12 , r, n;
    int prime = 1 ;
    int d = 1 ;
    cout << " Teclea un número entero positivo: " << endl;
    cin >>r;
    while {
        while {
            if (r% 2 == 0 ) r = r + 1 ;
            for (n = 2 ; n <= r / 2 ; n++)
                if (r%n == 0 )
                {primo = 0 ;
                   r = r + 2 ;
                }
                else primo = 1 ;
        } while (prime = 0 );
        if (r <s) {s = sr;}
        else {s = s- 1 ;}
        if (s == 0 ) { break ;}
        else {r = r + 2 ;}
    } while (d = 1 );
    
    cout << " S = " << r << endl;
    return  0;
}
